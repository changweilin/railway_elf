{
  "name": "railway-elf",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:rail-data": "node scripts/fetch-rail-shapes.mjs",
    "check:timing": "node scripts/check-train-timing.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
